{
  "rules": [
    {
      "allowed": {
        "origins": ["*"],
        "methods": ["GET", "HEAD"],
        "headers": ["range", "if-match"]
      },
      "exposeHeaders": ["etag", "content-length", "content-range", "accept-ranges"],
      "maxAgeSeconds": 86400
    }
  ]
}
